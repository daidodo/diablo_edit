#include "StdAfx.h"
#include "LeftData.h"

CLeftData::CLeftData(void)
{}

CLeftData::~CLeftData(void)
{}

BOOL CLeftData::ReadFile(CFile & cf)
{
	UINT len = UINT(cf.GetLength());
	len -= UINT(cf.GetPosition());
	if(len > 0){
		bData.resize(len);
		cf.Read(&bData[0],len);
	}
	return TRUE;
}

